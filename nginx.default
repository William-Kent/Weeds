# nginx.default

server {
    listen 8000;
    server_name example.org;

    location / {
        proxy_pass http://127.0.0.1:8000;
    }
    location /static {
        root /opt/app/in_the_weeds;
    }
    location /media {
        alias /opt/app/in_the_weeds/media;
    }
}